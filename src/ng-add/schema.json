{
  "$schema": "http://json-schema.org/schema",
  "id": "awesome-candy-enterprises",
  "type": "object",
  "properties": {
    "projectName": {
      "type": "string",
      "description": "The name of the projects root directory (will be determined automatically when not specified)"
    },
    "docFormat": {
      "description": "The stylesheet format to be used.",
      "type": "string",
      "default": "scss",
      "enum": [
        "scss",
        "css"
      ]
    }
  }
}
